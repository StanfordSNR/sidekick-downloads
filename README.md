# Packrat, a Sidekick Protocol for Data Downloads

1. Install dependencies in `deps/`.
2. Run emulation benchmarks in `emulation/`.
3. Run full experiments -- including those from the CoNEXT '26 paper -- in `notebook/`.
